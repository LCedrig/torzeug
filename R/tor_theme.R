#' Torben Theme
#'
#' Simply loads Torbens favorite Theme

tor_theme <- theme_set(theme_bw())

theme_update(aspect.ratio = 1.1,
             axis.title = element_text(size = 13),
             axis.text = element_text(size =13))
